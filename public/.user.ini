; PHP per-directory settings for the public application entry point.
; These override the system defaults for this application directory only.
upload_max_filesize = 20M
post_max_size = 50M
max_file_uploads = 50
